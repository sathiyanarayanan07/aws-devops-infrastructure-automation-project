pipeline {
    agent any

    stages {
        stage('checkout'){
            steps {
                checkout scm
            }
    }
    stage('Test') {
        steps {
            sh 'python3 --version'
            sh 'echo "Running application tests"'
        }
    }
    
    stage('Docker Build'){
        steps {
            sh ' docker build -f docker/Dockerfile -t aws-devops-app:v1 .'
        }
    }
    }
}